class Sum_CL
{
    public static void main (String numbers[])
    {
        //to sum two numbera via commandline
        int a = Integer.parseInt(numbers[0]);
        int b = Integer.parseInt(numbers[1]);

        System.out.println("Sum : " + (a+b));
    }
}